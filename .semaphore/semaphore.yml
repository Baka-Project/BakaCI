version: v1.0
name: semaphore
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804

blocks:
  - name: "Runner"
    run:
        when: "branch = '<name of branch repo'"
    task:
      jobs:
      - name: Start
        commands:
          - bash setup
